# Copyright 2026ff novatechflow (Alexander Alten)
# SPDX-License-Identifier: PolyForm-Shield-1.0.0
.DS_Store
# built binary
/polydisplayd
/polydisplay
/polydisplayd.exe
# toolchain dropped by install.sh when Go is missing
/.go/
# runtime logs
*.log
# live kiosk state (wallet, local watchlist). never commit.
/config.json
# local env (copy from .env.example); POLYDISPLAY_ASSETS lives here
/.env
