CACHE MANIFEST
# polyDisplay offline cache
# Bump this version string to push an update to already-cached iPads.
# v4 - 2026-07-31 (pull-to-refresh reload gesture)

CACHE:
index.html

# Everything else (the live APIs) must always go to the network.
NETWORK:
*
